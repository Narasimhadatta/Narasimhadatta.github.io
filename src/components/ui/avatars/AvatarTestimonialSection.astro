---
const { src, alt } = Astro.props;

interface Props {
  src: string;
  alt: string;
}
---

<div class="flex-shrink-0">
  <img
    class="size-10 rounded-full sm:h-[4rem] sm:w-[5rem]"
    src={src}
    alt={alt}
    loading="lazy"
  />
</div>
